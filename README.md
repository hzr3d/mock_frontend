# mock_frontend

A minimal React app that calls `mock_api`.

## Local run
```bash
npm ci
npm start
```

Set API URL with:
- `REACT_APP_API_BASE_URL` (defaults to `http://localhost:8000`)

## Test
```bash
npm test -- --watchAll=false
```
